#include <bits/stdc++.h>

using namespace std;

int main(void){
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int n;
	long long a = 0;
	cin >> n;
	
	while(n--){
		a += 4;
	}
	
	cout << a;
	return 0;
}